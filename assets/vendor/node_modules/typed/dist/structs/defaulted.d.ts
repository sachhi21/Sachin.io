import { Struct } from "../types";
/**
 * Creates a new struct that will return a default value if the input is undefined.
 */
export declare const defaulted: <T>(struct: Struct<T, any>, defaultValue: T) => Struct<T, any>;
