import { Infer, Shape, Struct } from "../types";
/**
 * Creates a new struct that accepts an object with the given shape.
 */
export declare const object: <S extends Shape>(shape: S, msg?: string) => Struct<Infer<S>, any>;
