import { Struct } from "../types";
/**
 * Creates a new struct that accepts either the specified struct or null.
 */
export declare const nullable: <T>(struct: Struct<T, any>) => Struct<T | null, any>;
