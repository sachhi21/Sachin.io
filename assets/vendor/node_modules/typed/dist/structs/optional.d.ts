import { Struct } from "../types";
/**
 * Creates a new struct that accepts either the specified struct or undefined.
 */
export declare const optional: <T>(struct: Struct<T, any>) => Struct<T | undefined, any>;
