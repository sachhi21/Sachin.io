import { Struct } from "../types";
/**
 * Creates a new struct that accepts a number.
 */
export declare const number: (msg?: string) => Struct<number>;
/**
 * Creates a new struct that will try to parse the input as a number.
 */
export declare const asNumber: (msg?: string | undefined) => Struct<number>;
