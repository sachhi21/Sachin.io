import { Struct } from "../types";
/**
 * Creates a new struct that accepts a date.
 */
export declare const date: (msg?: string) => Struct<Date>;
/**
 * Creates a new struct that will try to parse the input as a date.
 */
export declare const asDate: (msg?: string | undefined) => Struct<Date>;
