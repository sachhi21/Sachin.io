import { Struct } from "../types";
/**
 * Creates a new struct that accepts a string.
 */
export declare const string: (msg?: string) => Struct<string>;
/**
 * Creates a new struct that will try to parse the input as a string.
 */
export declare const asString: (msg?: string | undefined) => Struct<string>;
